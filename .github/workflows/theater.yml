# Theater gate — blocks verification theater INTRODUCED by a change, never the
# backlog this repo already carries. Green on arrival by construction.
#
# No secrets, no tokens, no organisation settings: the source repo is public.
# github.com/credit-union-2-0-llc/cu-agent-standards

name: Theater gate

on:
  push: { branches: [main] }
  pull_request: {}

jobs:
  theater:
    uses: credit-union-2-0-llc/cu-agent-standards/.github/workflows/reusable-theater.yml@main
